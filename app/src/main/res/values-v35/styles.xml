<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 15 (API 35)+ enables edge-to-edge by default when targeting SDK 35.
      That draws content under the status bar / ActionBar and breaks these sample layouts.
      Opt out until layouts are migrated to WindowInsets.
    -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
